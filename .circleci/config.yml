version: 2.1
workflows:
  testing:
    jobs:
      - vm_service_test

jobs:
  vm_service_test:
    docker:
      - image: 'cimg/node:14.10.1' # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory 470.45 MiB
      - run: echo "hello world" # run the `echo` command
